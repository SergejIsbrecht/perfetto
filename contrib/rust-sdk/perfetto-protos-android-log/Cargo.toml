[package]
edition = "2024"
name = "perfetto-protos-android-log"
version = "1.0.0"
authors = ["Sergej Isbrecht <sergej.isbrecht@gmail.com>"]
description = "Extra protobuf bindings for Android log events"
readme = "README.md"
keywords = [
    "tracing",
    "perfetto",
]
categories = ["development-tools::profiling"]
license = "Apache-2.0"
homepage = "https://www.perfetto.dev"
repository = "https://github.com/google/perfetto"

[features]
default = ["vendored"]
vendored = ["perfetto-sdk/vendored"]

[dependencies]
perfetto-sdk = { path = "../perfetto", version = "1", default-features = false }
paste = "1"
